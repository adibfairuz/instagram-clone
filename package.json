{
  "name": "instagram-clone",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "lib"
    ]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run --parallel build"
  },
  "devDependencies": {
    "lerna": "^5.1.5"
  }
}
